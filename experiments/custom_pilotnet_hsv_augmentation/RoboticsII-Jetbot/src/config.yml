model:
  # Absolute path to the exported ONNX model on the JetBot.
  # Replace with the path where you copy jetbot_model.onnx (OPSET 11).
  path: '/home/jetbot/Student/RoboticsII-Jetbot/src/jetbot_model.onnx'

robot:
  max_speed: 0.22
  max_steering: 0.5
  differential:
    left: 0.0
    right: 0.0