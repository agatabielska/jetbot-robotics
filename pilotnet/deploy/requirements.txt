# Bot-side runtime. Most of these come preinstalled on the Waveshare
# JetBot image. Install any missing pieces with:
#     pip3 install --user -r requirements.txt
#
# Note: the `jetbot` SDK is provided by the bot image itself, not pip,
# so it is NOT listed here.

onnxruntime>=1.10
numpy
opencv-python
pyyaml
